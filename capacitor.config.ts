import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.list.exercise',
  appName: 'exercise',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
